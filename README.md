# pizza-assignment

Simple Pizza Ordering System built with Angular.

The app lets users choose a pizza size, select toppings, review pricing, and see promotional offers applied in a cart summary. Pricing updates dynamically as the order changes.
